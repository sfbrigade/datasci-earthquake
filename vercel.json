{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "ignoreCommand": "test -n \"$(git log -1 | grep 'skip')\"",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
