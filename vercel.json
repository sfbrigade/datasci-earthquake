{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "ignoreCommand": "test -n \"$(git branch --show-current | grep 'ci_skip_vercel_build')\"",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
