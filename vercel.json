{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "ignoreCommand": "! git log -1 | grep '\[deploy-vercel-preview\]'",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
