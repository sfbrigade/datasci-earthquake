{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "buildCommand": "echo $VERCEL_ENV; test $VERCEL_ENV != 'production'; next build",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
