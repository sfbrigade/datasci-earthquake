{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "buildCommand": "echo $VERCEL_ENV; test $VERCEL_ENV != 'production'; echo $?; test $VERCEL_ENV == 'preview'; echo $?; next build",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
