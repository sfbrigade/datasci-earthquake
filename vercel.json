{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "buildCommand": "! (git log -1 | grep '\\[deploy-vercel-preview\\]'); echo $?; next build",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
