{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "ignoreCommand": "test $VERCEL_ENV != 'production' && ! (git log -1 | grep -i '\\[deploy-vercel-preview\\]')",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
