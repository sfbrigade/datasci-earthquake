{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "ignoreCommand": "test -n \"$(echo 'skip' | grep 'skip')\"",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
