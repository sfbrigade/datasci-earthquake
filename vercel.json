{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "buildCommand": "git log -1; next build",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
