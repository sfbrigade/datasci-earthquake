{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"      
    }
  ],
  "ignoreCommand": "test -n \"$(echo 'ci_skip_vercel_build' | grep 'ci_skip_vercel_build')\"",
  "functions": {
    "api/**": {
      "maxDuration": 20
    }
  }
}
