# Copilot Instructions

Use `AGENTS.md` in the repository root as the primary instruction source for this project.

When there is any conflict between local assumptions and repository conventions, prefer the conventions in `AGENTS.md`.
